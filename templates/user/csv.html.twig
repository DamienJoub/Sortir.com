{% extends 'base.html.twig' %}


{% block main %}
    {{ form_start(csvForm) }}
    <div class="form-button">
        {{ form_widget(csvForm) }}
        <button type="submit" class="btn btn-primary" >Ajouter</button>
    </div>
    {{ form_end(csvForm) }}
{% endblock %}